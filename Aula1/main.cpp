#include <iostream>

using namespace std;

int main(){
 
  cout << "oie" << endl;// Comando para exibir do C++ (´mais recomendavel usar esse por ser mais atual ) tbm para quebrar linha usa-se o endl ou dentro das aspas o /n
  printf("Olá mundo!!!");// Comando para exibir um item na tela da linguagem C
  system("pause");//Pausa o app ate o usuario digitar algo
  return 0;
}
